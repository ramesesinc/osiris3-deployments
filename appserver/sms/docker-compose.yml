version: "3"

services:
  cloud-sms-server:
    container_name: cloud-sms-server
    image: ramesesinc/osiris3-core:1.02
    restart: always
    logging:
      driver: "json-file"
      options:
        max-file: "5"
        max-size: 10m
    environment:
      TZ: "Asia/Manila"
    env_file:
      - ./env.conf
      - ../../_res/env.conf
      - ../../_custom/env.conf
    ports:
      - "9070:8070"
    volumes:
      - ./plugins:/enterprise/plugins

      - ../../_custom/datasources/smsds:/services/datasources/mainds
      - ../../_custom/output:/apps/server/output
